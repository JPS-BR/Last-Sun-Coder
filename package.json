{
  "name": "last-sun-coder",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run -w @lsc/core build && npm run -w @lsc/cli build",
    "dev": "npm run -w @lsc/cli dev"
  }
}

